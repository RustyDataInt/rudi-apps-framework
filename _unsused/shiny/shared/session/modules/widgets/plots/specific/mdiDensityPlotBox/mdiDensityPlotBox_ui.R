#----------------------------------------------------------------------
# UI components for plotting one or more frequency distributions
#----------------------------------------------------------------------

# module ui function
mdiDensityPlotBoxUI <- function(id, title, ...) {
    staticPlotBoxUI(
        id, 
        title = title,
        ...       
    )
}
